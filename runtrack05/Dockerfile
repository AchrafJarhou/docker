# Utilisation de l'image officielle PHP avec Apache
FROM php:8.2-apache

# Copie du fichier index.php dans le dossier par défaut d'Apache
COPY index.php /var/www/html/

# Le port 80 est exposé par défaut dans cette image
EXPOSE 80